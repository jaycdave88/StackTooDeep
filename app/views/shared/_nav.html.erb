  <nav class="main-nav">
    <li><%= link_to "Home", root_path %></li>
    <% if session[:user_id] != nil%>
    <li><%= link_to "Profile", show_user_path(session[:user_id])%></li>
    <%end%>
    <%if session[:user_id] == nil %>
        <li><%= link_to "Sign Up", new_user_path %></li>
        <li><%= link_to "Log In", new_session_path(session) %></li>
    <% else %>
        <li><%= link_to "Log Out", delete_session_path(session) %></li>
    <%end%>

  </nav>